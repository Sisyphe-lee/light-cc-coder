{
  "name": "light-cc-coder",
  "version": "0.1.3",
  "description": "A lightweight Claude Code-style TypeScript coder harness.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist",
    "install.sh",
    "README.md",
    "README.zh-CN.md"
  ],
  "bin": {
    "lightcc": "dist/main.js",
    "light-cc": "dist/main.js",
    "light-cc-coder": "dist/main.js"
  },
  "scripts": {
    "build": "node scripts/build-dist.mjs",
    "prepare": "node scripts/build-dist.mjs",
    "test": "bun test --path-ignore-patterns='references/**' --path-ignore-patterns='sandbox-runtime/**' --path-ignore-patterns='WebRepo/**'",
    "typecheck": "tsc --noEmit"
  },
  "optionalDependencies": {
    "@anthropic-ai/sandbox-runtime": "^0.0.52"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
